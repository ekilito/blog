hexo.on("ready", () => {
    const { version } = require("../../package.json");
    hexo.log.info(`
  ===================================================================
  马上就好！今天也是元气满满的一天呐~

                            ${version}
  ===================================================================`);
});